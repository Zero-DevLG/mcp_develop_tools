[project]
name = "mcp-fastmcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LGabriel", email = "lverdiguel@megatravel.com.mx" }
]
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.4.7",
    "pymysql>=1.2.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
mcp-fastmcp = "mcp_fastmcp:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
